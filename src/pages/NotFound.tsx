import React from "react";
import styled from "styled-components";

const NotFound: React.FC = () => {
  return (
    <NotFoundLayout>주소가 잘 못 되었어요~ 다시 확인해주세요~</NotFoundLayout>
  );
};

export default NotFound;

const NotFoundLayout = styled.div``;
