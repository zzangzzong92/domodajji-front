import React from "react";
import styled from "styled-components";

const Setting: React.FC = () => {
  return <SettingLayout>설정</SettingLayout>;
};

export default Setting;

const SettingLayout = styled.div``;
