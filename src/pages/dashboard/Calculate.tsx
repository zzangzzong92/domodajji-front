import React from "react";
import styled from "styled-components";

const Calculate: React.FC = () => {
  return <AccountBookLayout>정산</AccountBookLayout>;
};

export default Calculate;

const AccountBookLayout = styled.div``;
