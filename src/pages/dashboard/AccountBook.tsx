import React from "react";
import styled from "styled-components";

const AccountBook: React.FC = () => {
  return <AccountBookLayout>장부</AccountBookLayout>;
};

export default AccountBook;

const AccountBookLayout = styled.div``;
