import React from "react";
import styled from "styled-components";

const Reservation: React.FC = () => {
  return <ReservationLayout>약속파트</ReservationLayout>;
};

export default Reservation;

const ReservationLayout = styled.div``;
